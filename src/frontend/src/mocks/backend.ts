import type { backendInterface } from "../backend";

const mockPrincipal = { toText: () => "aaaaa-aa", _isPrincipal: true } as any;

export const mockBackend: backendInterface = {
  assignCallerUserRole: async () => undefined,
  claimInitialAdmin: async () => true,
  clearFlaggedAffiliate: async () => true,
  createEngine: async () => BigInt(1),
  deleteEngine: async () => undefined,
  deleteUserData: async () => true,
  deployApp: async () => "App successfully deployed!",
  distributeAcrossProviders: async () => undefined,
  distributeAndGetScore: async () => ({
    updatedEngines: [],
    overallResilienceScore: BigInt(72),
  }),
  getAdminAnalytics: async () => ({
    freeCount: BigInt(3),
    businessCount: BigInt(2),
    totalWaitlist: BigInt(10),
    totalEngines: BigInt(5),
    totalUsers: BigInt(7),
    proCount: BigInt(1),
    enterpriseCount: BigInt(1),
  }),
  getBillingEvents: async () => [],
  getCallerUserProfile: async () => null,
  getCallerUserRole: async () => "user" as any,
  getContentSettings: async () => ({
    demoModeEnabled: true,
    announcementBanner: "Caffeine v3.0 is live",
    affiliateEnabled: true,
  }),
  getCostSummary: async () => ({
    totalCost: 1.4,
    engineCosts: [[BigInt(0), 0.25], [BigInt(1), 0.4], [BigInt(2), 0.75]],
  }),
  getEngine: async () => ({
    id: BigInt(0),
    cpu: BigInt(4),
    ram: BigInt(16),
    status: "running",
    costPerHour: 0.25,
    provider: "AWS",
    ownerId: mockPrincipal,
    storage: BigInt(100),
    name: "LockFree",
    createdAt: BigInt(Date.now()),
    resilienceScore: BigInt(82),
  }),
  getFlaggedAffiliates: async () => [],
  getMigrationHistory: async () => [],
  getMySubscription: async () => "enterprise",
  getPublicContentSettings: async () => ({
    demoModeEnabled: true,
    announcementBanner: "Caffeine v3.0 is live",
    affiliateEnabled: true,
  }),
  getReferralCount: async () => BigInt(0),
  getUsageSummary: async () => ({
    enginesCount: BigInt(3),
    deploymentsThisMonth: BigInt(7),
    migrationsThisMonth: BigInt(2),
  }),
  getUserProfile: async () => null,
  getWaitlistEntries: async () => [],
  inviteSeat: async () => undefined,
  isCallerAdmin: async () => true,
  joinWaitlist: async () => true,
  listAllUsers: async () => [
    { tier: "enterprise", principalId: mockPrincipal },
  ],
  listEngines: async () => [
    {
      id: BigInt(0),
      cpu: BigInt(4),
      ram: BigInt(16),
      status: "running",
      costPerHour: 0.25,
      provider: "AWS",
      ownerId: mockPrincipal,
      storage: BigInt(100),
      name: "LockFree",
      createdAt: BigInt(Date.now()),
      resilienceScore: BigInt(82),
    },
    {
      id: BigInt(1),
      cpu: BigInt(8),
      ram: BigInt(32),
      status: "running",
      costPerHour: 0.4,
      provider: "GCP",
      ownerId: mockPrincipal,
      storage: BigInt(250),
      name: "GravityLock",
      createdAt: BigInt(Date.now()),
      resilienceScore: BigInt(91),
    },
    {
      id: BigInt(2),
      cpu: BigInt(16),
      ram: BigInt(64),
      status: "running",
      costPerHour: 0.75,
      provider: "Azure",
      ownerId: mockPrincipal,
      storage: BigInt(500),
      name: "HydraDNS",
      createdAt: BigInt(Date.now()),
      resilienceScore: BigInt(74),
    },
  ],
  listSeats: async () => [],
  migrateEngine: async () => undefined,
  migrateEngineWithDetails: async () => ({
    id: BigInt(0),
    status: "completed",
    engineId: BigInt(0),
    savedPerMonth: 73.0,
    appCount: BigInt(2),
    toProvider: "GCP",
    timestamp: BigInt(Date.now()),
    fromProvider: "AWS",
    engineName: "LockFree",
  }),
  populateDemoData: async () => undefined,
  removeSeat: async () => undefined,
  reportReferral: async () => ({ __kind__: "ok", ok: BigInt(1) }),
  saveCallerUserProfile: async () => undefined,
  saveContentSettings: async () => undefined,
  sendMessage: async () => "Sure! I can help you with that. Just let me know what you need.",
  setUserTier: async () => undefined,
  updateEngineStatus: async () => undefined,
  upgradeSubscription: async () => undefined,
  _getMigrationHistoryForTests: async () => [],
  _initializeAccessControl: async () => undefined,
};
